## [Swift] guard let / if let

> Swift는 Null Exception 오류를 피하기 위하여, Optional에 관한 기법을 지원한다.

가장 대표적으로 **if let** 과 **guard let** 이 있다.





```

```





```

```



if let은 블록안에서 사용이 가능하고,

guard let은 일반 변수처럼 사용이 가능하지만, 오류가 났을 시 예외처리 해주는 구문이 있다.

> 간단한 처리를 할 경우 if let을 ,
>
> 여러가지 변수 연산이 필요할 경우 guard let을 사용한다.